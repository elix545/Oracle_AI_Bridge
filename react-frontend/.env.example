# Environment variables for React Frontend
# This file is required by Docker Compose but the frontend uses Vite's built-in env handling

# Development mode (Vite will handle this automatically)
NODE_ENV=development

# Frontend port (matches docker-compose.yml)
PORT=5173

# API configuration (handled by Vite proxy in development)
VITE_API_BASE_URL=/api
